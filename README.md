# 🚀 HTML CSS JS LEARNING

A comprehensive and tracked repository of my frontend development progress. This codebase documents my continuous learning journey and structured approach to mastering frontend technologies, starting from basic HTML to Advanced Asynchronous JavaScript.

## 🎯 Purpose of this Repository
This repository is not a single project, but a structured code diary. I use this to experiment, revise concepts, and build a strong foundation before jumping into modern frameworks like React.

## 📂 Master Directory Index

Since the folders are logged sequentially by practice sessions (`code1` to `code138`), use the map below to navigate through specific technical topics:

### 🌐 HTML5 Fundamentals (`code1` - `code22`)
* **Core Concepts:** HTML Entities, Semantic Tags, Attributes.
* **Structuring Data:** Tables, Forms (GYM Form), Media Tags (Audio/Video).
* **Mini Implementations:** Song Playlist, Timetable Project.

### 🎨 CSS3 & Styling Architecture (`code23` - `code93`)
* **Styling Basics:** Inline, Internal, External CSS, Selectors, Box Model (`code23` - `code41`).
* **Advanced Visuals:** Pseudo-classes & Elements, Gradients, Transitions, Animations (`code42` - `code50`).
* **Modern Layouts:** Flexbox & CSS Grid architectures, CSS Positions, Z-index, Transform (`code51` - `code82`).
* **Framework Integration:** Tailwind CSS Utility Classes (`code83` - `code85`).
* **Practice:** Cloth Shop Project and intensive CSS revisions.

### ⚡ JavaScript: Core Concepts (`code94` - `code123`)
* **Language Fundamentals:** Variables, Datatypes, Operators, Conditionals, Loops (`code94` - `code108`).
* **Data Structures:** Arrays, Objects, JSON parsing (`code109` - `code114`).
* **Functions & Scope:** Arrow Functions, IIFE, Execution Context, Hoisting (`code115` - `code122`).
* **Advanced Concepts:** Closures, Higher-Order Functions (`code123`).

### ⚙️ JavaScript: DOM & Asynchronous Programming (`code124` - `code138`)
* **DOM Manipulation:** DOM Selectors, Event Listeners, CRUD Operations in DOM (`code124` - `code132`).
* **Async JS:** Handling Callback Hell, Promises, Async/Await (`code133` - `code136`).
* **Object-Oriented JS:** Prototypes, Classes, Strict Mode, `this` keyword, `call`, `bind`, `apply` (`code137` - `code138`).

---
*Building solid engineering fundamentals.*
